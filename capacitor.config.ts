import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apps.socialconnect',
  appName: 'chat-socket',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
